# Typing Speed Analyzer

A React-based typing speed test with:

- Word-by-word accuracy calculation
- WPM calculation
- Leaderboard stored in localStorage
- Timer and result buttons
- Random quote generation
